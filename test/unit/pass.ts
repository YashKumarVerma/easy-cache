/* eslint-disable import/no-extraneous-dependencies */
import { expect } from 'chai'
import { describe, it } from 'mocha'

describe('Testing Environment', () => {
  it('should pass if setup correctly', () => {
    expect(true).to.equal(true)
  })
})
